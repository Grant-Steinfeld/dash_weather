# Generated by the PowerShell module PSAdvantage
# Install-Module -Name PSAdvantage

# Template for basic PowerShell automation
name: Custom PowerShell Automation - Linux

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run the scripts
        shell: pwsh
        run: |
          sudo pip install matplotlib
          sudo pip install pandas

          python CreateGraph.py
          python try.py

          #.\ThePoshWay.ps1
      - name: Commit changes
        uses: EndBug/add-and-commit@v4 
        with:
          author_name: Grant Steinfeld
          author_email: grant.steinfeld.tech@gmail.com
          message: "Auto updating my dash"
          add: "*.png"
        env:
         GITHUB_TOKEN: ${{secrets.GH_WEATHER_DASH2 }} 
